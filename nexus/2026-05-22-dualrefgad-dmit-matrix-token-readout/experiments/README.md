# Experiments

- `scripts/v0_set_dmit_probe.py`: first V0 / Set-D-MiT runner-registered probe.
- `configs/v0_set_dmit_probe.yaml`: runner-validatable profile=probe config.
- `outputs/`: pulled JSON outputs and progress artifacts.
- `logs/`: pulled remote logs.

All execution must go through `experiment-runner` registration and tracked launch. True anomaly labels are diagnostic-only unless a future document explicitly states otherwise and user approves.
# Experiments

This directory stores configs, scripts, outputs, and plots for the profile-token readout investigation.

Hard rule: any scientific experiment must be run through `experiment-runner`. Manual runs may only be recorded as smoke tests or protocol deviations.

Initial planned probe:

- `profile_token_row_reader_probe`: row-profile token readout under fixed C-LEG3 / old-exact reference regime.
